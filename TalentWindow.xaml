<Window x:Class="ProverbTeleprompter.TalentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TalentWindow" Height="300" Width="300"
        xmlns:Prompter="clr-namespace:ProverbTeleprompter" WindowStyle="None">
    <Grid>
        <Border  RenderTransformOrigin="0.5,0.5">
            <Border.Background >
                <VisualBrush Visual="{Binding Source={x:Static Prompter:MainWindow.PromptView}}">


                </VisualBrush>
            </Border.Background>
            <Border.RenderTransform>
                <TransformGroup>
                    <RotateTransform x:Name="TalentRotate" Angle="0" CenterX="0" CenterY="0"/>
                    <ScaleTransform x:Name="TalentScale" ScaleX="1" CenterX="0" CenterY="0" />
                </TransformGroup>
            </Border.RenderTransform>
        </Border>
    </Grid>
</Window>
